{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "seed": "node prisma/seed.js",
    "sample-data": "node prisma/sample-data-seed.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:ctrf": "jest --ci"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "latest",
    "express-session": "^1.18.1",
    "multer": "^2.0.1",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.9.0",
    "supertest": "^7.1.1",
    "jest-ctrf-json-reporter": "^1.0.0"
  },
  "jest": {
    "reporters": [
      "default",
      ["jest-ctrf-json-reporter", { "outputDir": "ctrf", "outputFile": "ctrf-report.json" }]
    ]
  }
}
